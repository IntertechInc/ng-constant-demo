angular.module("myapp.config", [])

.constant("BASE_SERVICE_URL", "#{BaseServiceUrl}")

.constant("MAX_UPLOAD_FILE_SIZE", "#{MaxUploadFileSize}")

.constant("SESSION_TIMEOUT_MINUTES", "#{SessionTimeoutMinutes}")

.constant("SESSION_WARN_SECONDS", "#{SessionWarnSeconds}")

.constant("SUPPORT_EMAIL_ADDRESS", "#{SupportEmailAddress}")

.constant("SUPPORT_PHONE_NUMBER", "#{SupportPhoneNumber}")

.constant("SUPPORT_PHONE_WORD", "#{SupportPhoneWord}")

;